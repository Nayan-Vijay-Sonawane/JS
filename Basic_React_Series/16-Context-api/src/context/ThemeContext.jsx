import { createContext } from 'react'

const ThemeDataContext

const ThemeContext = (props) => {
    return (
        <div>
            {props.children}
        </div>
    )
}

export default ThemeContext;