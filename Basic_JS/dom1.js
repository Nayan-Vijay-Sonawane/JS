// Window: 
// Its a global object we can access it from anywhere it doesn't have any scope
// It is created by browser and it represent browser window
// window = DOM, BOM, JS Core(functionalities)

// BOM: 
// It allows JS to talk to browser about matter other than content of page.

// DOM:
// Document Object Model
// for eg we converted the entire code in JS object so we called that a document