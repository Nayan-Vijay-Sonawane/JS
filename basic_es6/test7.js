/*
- Ternary operator is used for adding the condition.
- shortcut way of writing if-else condition.
condition ? trueExpression : falseExpression
*/

const marks = 50;

const result = marks > 35 ? "Pass" : "Fail";
console.log(result);